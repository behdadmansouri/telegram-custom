From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Bmans <bmans054@uottawa.ca>
Date: Wed, 23 Sep 2026 15:05:38 -0400
Subject: [PATCH] Hide similar channels

Never fetch channel recommendations, so the block after joining, the profile
button and the profile tab all stay empty (they hide at count 0). Similar
bots are untouched.
---
 Telegram/SourceFiles/api/api_chat_participants.cpp | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/Telegram/SourceFiles/api/api_chat_participants.cpp b/Telegram/SourceFiles/api/api_chat_participants.cpp
index 6df9cba8..16ea30c2 100644
--- a/Telegram/SourceFiles/api/api_chat_participants.cpp
+++ b/Telegram/SourceFiles/api/api_chat_participants.cpp
@@ -859,6 +859,9 @@ void ChatParticipants::applyKicked(
 }
 
 void ChatParticipants::loadSimilarPeers(not_null<PeerData*> peer) {
+	if (peer->isBroadcast()) {
+		return; // telegram-custom: no "similar channels"
+	}
 	if (const auto i = _similar.find(peer); i != end(_similar)) {
 		if (i->second.requestId
 			|| !i->second.peers.more
-- 
2.55.0

